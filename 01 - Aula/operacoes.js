function soma(x, y) {
  return x + y;
}
  
function subtracao(x, y) {
  return x - y;
}
  
const multiplicacao = function(x, y) {
  return x * y;
}

const divisao = (x, y) => {
  return x / y
}

module.exports = {
  soma,
  subtracao,
  multiplicacao,
  divisao
};
  